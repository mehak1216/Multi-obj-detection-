# Recommended interpreter: Python 3.12 for Streamlit Community Cloud.
# Python 3.13+ can force incompatible source builds for this pinned scientific stack.
# Keep Ultralytics new enough for the default YOLO11 weights used by the app.
ultralytics==8.4.37
# Use a newer headless build with solid Python 3.12/Linux wheel support for Streamlit Cloud.
opencv-python-headless==4.10.0.84
numpy==1.26.4
pandas==2.2.2
PyYAML==6.0.1
yt-dlp==2024.8.6
streamlit==1.32.2
lap>=0.5.12
