# Recommended interpreter: Python 3.12 for Streamlit Community Cloud.
# Python 3.13+ can force incompatible source builds for this pinned scientific stack.
ultralytics==8.2.103
# Use the headless build for Streamlit/server deployments where GUI libs are unavailable.
opencv-python-headless==4.9.0.80
numpy==1.26.4
pandas==2.2.2
PyYAML==6.0.1
yt-dlp==2024.8.6
streamlit==1.32.2
